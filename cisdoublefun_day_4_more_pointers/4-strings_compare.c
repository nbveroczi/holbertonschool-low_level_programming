/* Compare Strings
This is a function that compares two strings.
*/
#include <string.h>
int strings_compare(char *s1, const char *s2)
{
  int result;
  result = strcmp(s1, s2);

  if(result < 0)

  return(result);


}
