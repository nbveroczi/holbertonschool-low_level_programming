#include "5-reverse_array.h"

/* this is a function that reverses the content of an array of integers */
void reverse_array(int *a, int n)
{
  int i;

  for (i = 0; src[i] != '\0'; i++)
    {
      dest[i] = src[i];
    }
  for ( ; i < src[i]; i++)
    {
      dest[i] = '\0';
    }
  return dest;
}
